# Pilotfish native Codex Multi-Agent V2 configuration.
# Merge these keys and tables into ~/.codex/config.toml.
model = "gpt-5.6-sol"

[features.multi_agent_v2]
enabled = true
# Total slots, including the root session: root + three children.
max_concurrent_threads_per_session = 4
